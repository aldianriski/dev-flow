---
owner: Tech Lead (Aldian Rizki)
last_updated: 2026-04-21
update_trigger: §22 spec changes; validation rule additions; output format revision
status: current
---

# Task Decomposer — Reference Spec

Supporting reference for `task-decomposer/SKILL.md`. Do not edit this file without updating §22 in `docs/blueprint/10f-task-decomposer.md`.

---

## Output Format Template

```markdown
## Task Decomposition — [Feature/Epic Title]

**Source**: [freeform | ticket JIRA-123 | prd docs/feature.md | epic "Name"]
**Scope-analyst impact**: [N] files, [layers], API change: [yes|no], Risk: [low|medium|high]

**Assumptions confirmed**:
1. [Assumption 1]

**Decomposition** ([N] tasks):

---

### P[N] — [Epic/Feature Theme]

- [ ] **TASK-[NNN]: [Title]** — [why it matters, one line]
  - `scope`: full | quick
  - `layers`: [comma-separated, validated against CLAUDE.md]
  - `api-change`: yes | no
  - `acceptance`: [measurable — "[action] returns/produces [result]"]
  - `tracker`: [URL if available | "none — [justification]"]
  - `risk`: low | medium | high
  - `depends-on`: TASK-[NNN] | none
  - `assumptions`: [task-specific assumptions, or "none"]

---

**Dependency graph**:
TASK-001 → TASK-002 → TASK-004
TASK-003 (independent)

**What I did NOT include** (out of scope based on your input):
- [item explicitly excluded]

---
Type 'approve' to write these tasks to TODO.md **Backlog**.
Type 'revise [task number] [correction]' to adjust a specific task.
Type 'split TASK-NNN' to further decompose a task.
Type 'merge TASK-NNN TASK-MMM' to combine two tasks.

> **Sprint formation**: tasks land in Backlog. Run `/lean-doc-generator` Sprint Promote to promote tasks to a sprint and create the sprint file.
```

---

## Assumption Registry Format

Present before Step 4 (risk scoring). Human must confirm or correct before decomposition proceeds.

```markdown
## Assumptions I am making (correct any that are wrong):

1. [Assumption 1] — based on: [reason / existing pattern in codebase]
2. [Assumption 2] — based on: [reason]

If any assumption is wrong, tell me now. If all acceptable, type 'decompose'.
```

---

## Granularity Rules

**Target task size**: 2–6 hours of focused implementation work
**Maximum task size**: 1 day — if larger, must split before output
**Minimum task size**: 30 minutes — if smaller, merge with adjacent task

**Splitting triggers** (any one is sufficient):
- Task touches more than 3 distinct files in different layers → split by layer
- Task has more than one independently verifiable acceptance criterion → split
- Task has a `depends-on` dependency → that dependency must be its own task

**Grouping rules**:
- Group 2–5 tasks per P-level/epic block in Backlog (never 1 alone — add adjacent task or note standalone reason)
- Order by: dependency first, then importance, then risk (high first)
- Sprint formation happens via `/lean-doc-generator` Sprint Promote, not here

---

## Validation Rules

All rules enforced before output is written to TODO.md. Hard stops — not warnings.

```
❌ TASK has no measurable acceptance criteria — "works correctly" is not accepted
❌ TASK layers not in the valid layer list from CLAUDE.md — reject and correct
❌ TASK scope is "hotfix" — task-decomposer never assigns hotfix scope
❌ Assumption made about auth/security without explicit human confirmation — flag CRITICAL
❌ TASK estimated >1 day without being split — must split before output
❌ P-level block has only 1 task — must add adjacent task or add standalone justification
❌ tracker is "none" without written justification — prompt for URL or explicit reason
❌ Two tasks have identical acceptance criteria — merge or differentiate
❌ --from-architecture invoked outside INIT Phase I-4 without explicit human confirmation — state context mismatch and require acknowledgment before proceeding
```

---

## Special Mode: `--from-architecture` (INIT Sprint 0 only)

Used exclusively during INIT mode Phase I-4. Skips clarification and assumption registry entirely. Uses Gate B architecture document as the full spec.

Generate Sprint 0 tasks covering:
1. Project entry point + health endpoint
2. Router + dependency injection setup
3. Authentication foundation (chosen auth pattern from Gate B)
4. Database connection + core entity schema (from Gate B domain model)
5. CI pipeline green (lint + typecheck + tests pass)

Apply risk scoring. Auth and DB tasks are always `risk: high`.
Group as Sprint 0. All tasks must be independently verifiable.
Present for human approval before writing to TODO.md.

---

## Ticket Credential Resolution

For ticket input (`JIRA-123`, Linear URL, GitHub issue URL):

```bash
LINEAR_API_KEY=...
JIRA_BASE_URL=...   # + JIRA_USER + JIRA_API_TOKEN
GITHUB_TOKEN=...
```

If credentials missing: ask human to paste ticket content directly. Do not block on missing credentials — degrade gracefully.
